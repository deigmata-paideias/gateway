# Input 02

记录日期：2026-07-15

## 归档范围

以下内容为用户给出的第二条产品需求原文。会话级 `AGENTS.md` 与 `environment_context` 属于执行约束，不作为产品需求归档。

## 用户输入原文

> 使用 schema 驱动的形式配置网关，支持通过 api 动态切换 backend。chat openai 用 go openai sdk，image 上如果 openai 存在 image sdk，则使用，dashscope 使用自建的即可。在实现方式上可以都通过 base64 传递给网关的调用方。
