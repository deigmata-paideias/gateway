# Input 05

记录日期：2026-07-15

## 归档范围

以下内容为用户给出的第二阶段设计复核与归档指令原文。会话级 `AGENTS.md` 与 `environment_context` 属于执行约束，不作为产品需求归档。

## 用户输入原文

> 复核修改点：1. 支持 openai 的 response api，用户权限系统暂时不需要，密钥管理需要的，但是不要和用户绑定。调用记录等保存到 sqlite 数据库中，以简单为主。token 和可用模型也可以从 api 接口查询到，以进行调用。同时 ai 网关要提供 token 的审计能力，以 token 粒度为单位即可。2. 保证单测覆盖率在 90% 以上。 3. 添加 mock e2e 验证。4. 适配 otel，暴露 ai 网关本身观测指标 5. 提供 docker compose 部署，并 yaml 中配置 healthness 探针，探索服务是否启动。以这些修改点为设计版本 02 归档。
